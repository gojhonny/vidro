---
title: 外部值
description: 密钥不进入受版本控制的 Vidro 源，而是用同名引用。
---

# 外部值

密钥值不得复制进受版本控制的 Vidro 源。密钥是外部引用：

```css
RESEND_API_KEY: $RESEND_API_KEY;
```

两边使用同一个名字。引用记录这个值来自 Vidro 文件之外。它不保存密钥本身。

解析顺序——进程环境、被忽略的 env 文件、CI 或部署平台——还没有决定。打进客户端的值，例如 `NEXT_PUBLIC_` 或 `VITE_` 变量，不会因为这样引用就变成机密。
