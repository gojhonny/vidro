# Skills

Reserved for repeatable Vidro implementation procedures.

Add skills only when a workflow has stabilized enough to be reusable; product decisions belong in `context/` and implementation invariants belong in `rules/`.
